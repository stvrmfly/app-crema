# Archive

Historical artifacts kept for provenance. Not actively maintained.

| File | What it is |
|---|---|
| `PRD.md` | Original Product Requirements Document. Written in Indonesian for an academic deliverable (Binus). Project name listed as **SimplePOS** — predates the rename to **Crema**. |
| `PRD.pdf` | Rendered PDF of `PRD.md`. |
| `generate_prd_pdf.py` | Python script that renders `PRD.md` → `PRD.pdf`. **Note:** the output path in line 143 is hardcoded to `/Users/cedric/erp-coffeeshop/PRD.pdf` — already stale (project lives at a different path now). Update the path if you need to re-render. |
| `project-template.md` | The original project brief. Self-described as "source of truth that Claude Code must read at the start of every session" — **superseded by `CLAUDE.md` at the repo root**. Kept for historical reference only. |

If none of this is useful to you anymore, the entire `archive/` folder can be deleted with no impact on the running app.
